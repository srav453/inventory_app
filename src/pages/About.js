
const About = ()=> {
    // const authctx = useContext(AuthContext)
    console.log("About Page");
    return (
        <div> <h1>Welcome to Inventory page</h1>
    <h2>Basically we have a inventory records if we work in shopping websites or 
    E-Commerce websites you can make a list of products with their price, by product name, 
    price and quantity.you can add in the inventory records, 
    later we can also reduce the quantity or change it by using react JS</h2>
    
    </div>
    );
    };
    export default About;